[project]
name = "azure-well-architected-framework-mindmap"
version = "1.0.0"
description = "A mindmap representation of the Azure Docs and Azure Well-Architected Framework."
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["pyyaml>=6.0.2", "typer>=0.17.4"]

[tool.uv]
dev-dependencies = ["black", "flake8", "isort", "pytest", "pytest-cov"]
